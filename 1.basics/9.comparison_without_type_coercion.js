// the === operator compares by value without any type coercion.

1 === "1"; // false

true === 1; // false

undefined === null; // false

//Commentts"
/* === : they have to exactly match(formating and the characters). no conversion to ASCII values during comparison
== : they do not need to exactly match but their int values should be the same
*/


console.log(`THE RESULT OF THE OPERATION ${5==="5"}!`)